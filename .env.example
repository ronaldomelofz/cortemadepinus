# ---------------------------------------------------------------
# Variaveis do servidor local MadePinus (sem custo)
# Copie para .env e ajuste as senhas
# ---------------------------------------------------------------

API_PORT=4000
JWT_SECRET=gere-com-node-e-crypto-randomBytes-48-hex
JWT_EXPIRES_IN=7d
MAX_UPLOAD_MB=20

# Origens autorizadas do front-end (separadas por virgula)
CORS_ORIGINS=https://cortemadepinus.netlify.app,http://localhost:5173

# Administrador criado automaticamente
ADMIN_NOME=Central de Servicos MadePinus
ADMIN_EMAIL=admin@madepinus.com.br
ADMIN_SENHA=troque-este-acesso

# Cloudflare Tunnel (gratuito, hostname FIXO no painel).
# Guia: docs/tunel-madepinus-24x7.md
#   .\scripts\instalar-tunel-madepinus.ps1 -IntegrarNetlify
TUNNEL_TOKEN=
# URL HTTPS publica da API (a mesma do Public Hostname no Cloudflare)
API_PUBLIC_URL=https://api.seudominio.com.br
