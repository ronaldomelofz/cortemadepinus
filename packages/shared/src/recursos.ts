/**
 * Interruptores de funcionalidade. O modelo do banco permanece; desligar aqui
 * esconde a funcao na interface e nos arquivos gerados ate o recurso ser
 * retomado.
 */
export const RECURSOS = {
  /** Fita de borda (C1/C2/L1/L2). Desligada por enquanto; religar no futuro. */
  fitaDeBorda: false,
} as const;
